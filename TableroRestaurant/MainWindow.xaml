<Window x:Class="TableroRestaurant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TableroRestaurant"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"  Background="#121212">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="#1E1E1E" Padding="10">
            <StackPanel>

                <TextBlock Text="🍽 PANEL DE PEDIDOS"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="{Binding Hora}"
                           FontSize="14"
                           Foreground="#AAAAAA"
                           HorizontalAlignment="Center"/>

            </StackPanel>
        </Border>

        <!-- CONTENIDO -->
        <ScrollViewer Grid.Row="1" Margin="10">

            <ItemsControl ItemsSource="{Binding Pedidos}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Border Width="250"
                                Height="150"
                                Margin="10"
                                CornerRadius="12"
                                Background="#2A2A2A"
                                BorderBrush="#3A3A3A"
                                BorderThickness="1">

                            <Grid Margin="10">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- NUMERO -->
                                <TextBlock Text="{Binding Numero}"
                                           FontSize="26"
                                           FontWeight="Bold"
                                           Foreground="#00E676"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"/>

                                <!-- ESTADO -->
                                <TextBlock Grid.Row="1"
                                           Text="{Binding Estado}"
                                           FontSize="14"
                                           Foreground="#DDDDDD"
                                           HorizontalAlignment="Center"
                                           Margin="0,5,0,10"/>

                                <!-- BARRA DE ESTADO -->
                                <Border Grid.Row="2"
                                        Height="12"
                                        CornerRadius="6"
                                        Margin="20,10,20,0">

                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Gray"/>

                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding Estado}" Value="PREPARANDO">
                                                    <Setter Property="Background" Value="Orange"/>
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding Estado}" Value="LISTO">
                                                    <Setter Property="Background" Value="LimeGreen"/>
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding Estado}" Value="ENTREGADO">
                                                    <Setter Property="Background" Value="DodgerBlue"/>
                                                </DataTrigger>

                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                </Border>

                            </Grid>

                        </Border>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

    </Grid>
</Window>
