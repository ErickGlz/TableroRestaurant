<Window x:Class="TableroRestaurant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TableroRestaurant"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🍳" FontSize="32" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="RESTAURANTE" FontSize="14" Foreground="Gray" FontWeight="Bold"/>
                    <TextBlock Text="FEITAN" FontSize="26" Foreground="#4CAF50" FontWeight="Black"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="¡Gracias por su preferencia!" 
                       FontStyle="Italic" FontSize="20" Foreground="White"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="12:45 PM" FontSize="24" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBlock Text="24/05/2026" FontSize="12" Foreground="Gray" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="🔔 PEDIDOS LISTOS" Foreground="#4CAF50" BorderBrush="#333333" Margin="0,0,0,10">
                    <ItemsControl Margin="10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" Rows="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="5" CornerRadius="8" BorderBrush="#4CAF50" BorderThickness="2" Background="#1A4314">
                                    <TextBlock Text="{Binding NumeroPedido}" FontSize="28" FontWeight="Bold" Foreground="White" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="⏳ EN PREPARACIÓN" Foreground="#FF9800" BorderBrush="#333333" Margin="0,10,0,0">
                    <ItemsControl Margin="10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" Rows="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="5" CornerRadius="8" BorderBrush="#FF9800" BorderThickness="1" Background="#2A1F0C">
                                    <TextBlock Text="{Binding NumeroPedido}" FontSize="24" Foreground="White" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#1A237E" CornerRadius="10" Margin="0,0,0,10" BorderBrush="#3F51B5" BorderThickness="1">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📋" FontSize="32" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="TURNO ACTUAL" FontSize="14" Foreground="LightGray" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="A-023" FontSize="48" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="10" Margin="0,10,0,0" Padding="15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ESTADOS" FontSize="12" Foreground="Gray" FontWeight="Bold" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <Ellipse Width="10" Height="10" Fill="#4CAF50" Margin="0,0,10,0"/>
                            <TextBlock Text="LISTO (Listo para recoger)" Foreground="White" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <Ellipse Width="10" Height="10" Fill="#FF9800" Margin="0,0,10,0"/>
                            <TextBlock Text="PREPARANDO (En cocina)" Foreground="White" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="🔊" FontSize="18" Foreground="Gray"/>
            <TextBlock Grid.Column="1" Text="Por favor mantenga el área libre. Gracias." 
                       Foreground="LightGray" HorizontalAlignment="Center" FontSize="14"/>
            <TextBlock Grid.Column="2" Text="💙" FontSize="18" Foreground="Gray"/>
        </Grid>
    </Grid>
</Window>
